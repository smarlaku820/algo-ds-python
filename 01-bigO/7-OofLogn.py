# O(log n) is almost as flat as O(1)
# And it is as efficient as O(1) and is far better than
# O(n^2) or O(n)

# And some sorting algorithms (merge/quick) run at O(n*logn) slightly
# better than O(n^2) and worse than O(n)

# O(n^2) < O(n*logn) < O(n) < O(logn) < O(1).
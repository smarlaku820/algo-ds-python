# Only operation is addition.
# Its a constant time operation.

# Summary.
# As n increases, O(1) is always a single or constant no of operations.
# Most efficient - O(1)
# As n increases, O(n) also increases proportionally.
# As n increases, O(n^2) increases exponentially.

def add_items(n):
    return n + n

add_items(4)